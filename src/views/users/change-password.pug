extends ../base

block content 
    if errorMessage 
        span=errorMessage
    form(method="post") 
        input(placeholder="기존 비밀번호", name="oldPwd",type="password")
        input(placeholder="새 비밀번호", name="newPwd",type="password")
        input(placeholder="새 비밀번호 확인", name="isNewPwd",type="password")
        input(type="submit", value="Change Password")